package model;

public class CargoPlane extends Plane {

    public CargoPlane(String model, int loadCapacity, int flightDistance, int maxSpeed, int fuelCapacity, int fuelUsagePerKilometer) {
        super(model, loadCapacity, flightDistance, maxSpeed, fuelCapacity, fuelUsagePerKilometer);
    }
}
